server:
  port: 3001

spring:
  datasource:
    password: password
    url: jdbc:postgresql://postgres:5432/taskmanager
    username: ujjwal
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
      show_sql: true
